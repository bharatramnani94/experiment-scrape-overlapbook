const puppeteer = require('puppeteer');

let scrape = async () => {
  // Actual Scraping goes Here...

  return Math.random();

  // Return a value
};

const result = await scrape();
console.log(result);